var shim = {{shim}};
var flash = {{flash}};
var sockets = {{sockets}};
var jsonp = {{jsonp}};

if (shim) {
    require("shim");
}

if (flash) {
    require("flash");
}

if (sockets) {
    require("sockets");
}

if (jsonp) {
    require("jsonp");
}
